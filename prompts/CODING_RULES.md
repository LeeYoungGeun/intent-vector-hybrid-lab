# 코딩 규칙(CODING_RULES)

- Python 기준(권장). 단, 대표님 팀 표준이 있으면 Node/Java로 전환 가능.
- 모듈 간 결합을 느슨하게(의존성 주입).
- 어댑터별 예외를 표준 에러로 래핑.
- 벤더 변경은 YAML 설정만 바꾸면 되게 설계.
- 네트워크 호출은 타임아웃/재시도(지수 백오프) 포함.
- 결과는 반드시 JSONL로 저장(대량 케이스 처리 용이).

